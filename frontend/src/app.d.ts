// Vereinfachte Typdefinitionen ohne Authentifizierung
declare global {
	namespace App {
		interface Locals {
			// Platzhalter-Eigenschaft, um ESLint-Warnung zu vermeiden
			_placeholder?: unknown;
		}
	}
}

export {};
